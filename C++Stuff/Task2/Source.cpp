#include "Queue.h"
#include <iostream>

bool addToQ(queue& Inp, char v, int size)
{
	for (int i = 0; i < size; i++)
	{
		if (v == [i])
	}
}

int main()
{
	std::string input;
	std::cout << "Enter the string" << std::endl;
	std::cin >> input;

	queue INPUT(input.length());

	for (int i = 0; i < input.length(); i++)
		INPUT.enqueue(input[i]);
	queue Distincts(30);

}
